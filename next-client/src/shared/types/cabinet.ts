export type CabinetInfo = CabinetElement[]


export type CabinetElement = {
    time: string
    subject: string
    type: string
    room: string
    teacher: string
    links: string
    text: string
}